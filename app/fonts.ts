import { Rubik, Open_Sans, Montserrat } from "next/font/google";

export const rubik = Rubik({
  subsets: ["latin"],
  variable: "--font-rubik",
});
export const openSans = Open_Sans({ variable: "--font-open-sands" });
export const montserrat = Montserrat({ variable: "--font-montserrat" });
